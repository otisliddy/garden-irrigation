node_modules/
*.js
!jest.config.js
# Lambda handlers are hand-written JS source (Node runtime, no bundling), not
# compiled CDK output — they must be tracked.
!lambda/**/*.js
*.d.ts
cdk.out/
.cdk.staging/
cdk.context.json
*.log
# device certs/keys must never be committed
certs/
*.pem
*.key
